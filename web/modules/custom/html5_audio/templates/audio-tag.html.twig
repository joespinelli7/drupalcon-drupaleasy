{#
/**
 * @file
 * Theme override to display an audio tag.
*/
#}

<audio controls {{ autoplay }} preload="metadata">
   {% for source in sources %}
       <source src="{{ source.src }}" type="{{ source.mimetype }}" />
   {% endfor %}
</audio>